# Next-market selection, frozen before the next outcome evaluation

Research-only,2026-09-22. Starting research head12de8d43. Live main8df2a5ba fetched; its delta from the research base touches only two Statcast read-performance scripts, neither an experiment input, evaluator nor contract. Continue in the existing PR187 branch; no new branch/PR, rebase, history rewrite, rollback or concurrent-file overwrite is needed. Old freeze SHAs stay reachable.

The accompanying read-only comparison inventories142 baseline artifacts and normalizes119 closed historical records, plus the exact PR186 F5 spread diagnostic. Every candidate has an artifact path and cohort label. External/forward/operational outcome files are inventory-only; their performance is not a ranking input. Previously selected formulas are retained; candidate-grid arrays are not searched for a higher percentage. Reference totals and run-threshold diagnostics remain clearly separate from real exact-line backtests. Missing coverage is NR, not inferred.

## Decision

**First: F1 ML.** Its preserved unified revisit is51/69=73.91%,six selected months,worst63.16%,all-OOF selection coverage3.5430% including78pushes (147selected). The archived conditional nonpush coverage is3.5222%; these denominators must not be mixed. The sample clears60 but is marginal, minimum month n2, and monthly stability fails. It has stronger unified near-target evidence than the remaining admitted markets, with exact F1 outcomes already certified and a materially new nonlinear kernel mechanism available. This does not certify or reuse the old102-feature model; its threshold0.625 and coefficients remain frozen.

**Second if F1 fails: F3 ML.** The prior2025 result55/77=71.43% has worst66.67%, but the broader rolling revisit202/320=63.13%,worst52.94% materially weakens it. Preserve both instead of selecting the more favorable season. An ordinal run-margin likelihood would be a distinct family if needed.

F5 ML's raw4/4 and F5 three-way2/2 are rejected as prioritization evidence. The F5 recent opponent-count model has just failed; no dispersion/penalty variant is warranted. F1/F3/F5 three-way references are further below target and/or unstable. NRFI's larger sample remains around54–56%, and its latest boosting has no selections. Game Totals has historical55.32%/n479 and unified53.48%/n804, with three mixed line sources requiring independent admission for a new replay; it is not preferred over an exact-outcome, line-free target.

F7 remains completely frozen. All missing exact-line period spreads, alternate spreads/totals and team totals are excluded. Lineup/participation-dependent work remains blocked. Already-qualified props and operational markets are outside this continuation; no external result is used to rank or rescue them.

## First new family

F1 ML: fixed Gaussian random-Fourier kernel logistic classifier over prior team F1 joint score distributions. The18-dimensional Hellinger representation contains9 smoothed score/allow categories for each team (0,1,2+ runs), constructed only from prior same-season games. This models nonlinear distribution-shape similarity rather than old candidate probabilities, nearest-neighbor voting, a QDA Gaussian class likelihood, a count-rate model or a changed Davidson strength model. Frequencies, seed, bandwidth, number of components, regularization, training schedule, burn-in and0.75 selection confidence are frozen in a separate contract before any real-data candidate evaluation.

Original historical gates remain accuracy75%,n60,at least5 selected months and worst-month65%. Monthly/LOMO/uncertainty diagnostics will also be reported. A base-gate pass stops for authorization; external does not open automatically. Failure is preserved without tuning, followed by the next admitted market in this predeclared priority order.
