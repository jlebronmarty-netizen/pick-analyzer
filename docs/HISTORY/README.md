# History Documentation

History documentation preserves superseded plans, older audits, prior release evidence and historical implementation records.

## Current Source Documents

Many V1, historical data, build-memory and legacy artifacts still live at the root of `docs/` and will be migrated in bounded batches.

## Migration Policy

Do not delete history documents automatically. Archive only when a newer canonical document exists and links are updated.
