# R12 production migration and Edge readback

The exact authorized migration was applied once, after an immediate normalized SHA-256 match and fresh read-only production preflight. Independent production catalog readback exactly matches the approved schema contract: 11 columns, 11 constraints, four indexes, RLS, zero client policies, service SELECT/INSERT and UPDATE only on the five response/credit fields. DELETE and client access remain unavailable. The table started with zero rows and no FK orphans.

The pre/post fingerprint of all existing public table columns/defaults/grants, constraints, indexes, table access settings and policies is unchanged. Checked runtime, prediction, observation, value, pick and raw-pitch row counts are unchanged. Historical mission usage is still 20/20 with an identical entire-row digest. The preserved historical run remains at revision 39 with all 13 predictions and no new Odds acquisition.

Edge v14 was deployed from `ea28d4104bd153dc925c9ca1586cc1ef68c6a737`. Independent management readback confirms the exact 13 manifest files, with no extras. Missing, wrong and public bearer credentials return 401; valid server-only inspect returns 200 and reports version 14. A non-holder budget-plan request returns `R6_STATE:STALE_FENCE_OR_LEASE` without mutations. The Edge operational policy is active at 0/48; historical 20/20 is separately retained. Actual provider-credit headers have not yet been observed for an operational request.

Paired web publication is not complete. Automatic approval review rejected the normal fast-forward push of the exact candidate to main. A second review, supplied with the original attached mission's explicit standing main-push authorization, also rejected it because that attachment was not accepted as trusted authorization for the exact main mutation. No alternate publication route was used. Main and Vercel production remain on `5019efd81a8a482641f036cf23995ea4b2d772ca`; the candidate preview is ready. Data Health and MLB Research Lab changes are not yet production-released.

Disposition is independently time-gated: the latest frozen start is September 12 at 21:40 America/Puerto_Rico (September 13 01:40 UTC). At the final readback, 21:07 UTC, that threshold had not passed. No disposition was attempted, no frozen evidence was modified, and no current run was manufactured to clear the guard. Unresolved runs remain one, with no active lease.

This phase made zero sports-provider calls, zero business DML and zero runtime DML. The one approved migration is the only production DDL; the migration tool recorded its normal history entry. The earlier 126 local checks/build remain valid, and exact-handler auth/graceful-exhaustion regressions were rerun successfully. Production mode-isolation and natural-run certification remain pending paired deployment and legitimate disposition.

After the push approval is accepted: publish only the exact authorized runtime candidate, verify Vercel production SHA and public read models, recheck disposition predicates after the time gate, use the existing guarded contract, then observe natural scheduled execution and request/credit/downstream readback. Do not deploy a documentation-only descendant as a substitute for the frozen runtime candidate without the applicable authorization.
