# R9 immutable market read and guarded resume

Local Gates 1-7 pass. Production deployment, resume, second-pass verification and the next natural scheduled execution remain pending. This audit does not claim that the failed run succeeded.

Starting package: `e3cfca7c54036130b6435be0da543eefaf32db43`.
Failed run: `automation-9756864d6172709d333ee55f5522d6d400b8983f0e2038a064bb05463b895c95`.
Frozen package: `b654a3d74cff473ab6917f15078a95925b3e2ab2`; freeze `2026-09-11T15:45:11.920Z`; failed revision50 at MARKET_PERSISTENCE.

Read-only production inventory confirms13 predictions,13 market mappings, paid Odds evidence and0 market observations. Private inventories retain every original identity and payload. The canonical JSON contains counts and aggregate identity/evidence digests, not raw samples or private storage contents. Mission Odds is5/20, with historical legacy acquisitions retained separately. This R9 local phase used no providers or production DML/DDL.

The production access contract is SELECT/INSERT allowed, UPDATE/DELETE denied on observations, values and Official Picks. PostgreSQL requires UPDATE permission for `SELECT FOR UPDATE`; the existing writer therefore failed42501 before inserting observations. The intended immutable permission model is retained. See PostgreSQL's [SELECT locking contract](https://www.postgresql.org/docs/current/sql-select.html) and [INSERT conflict handling](https://www.postgresql.org/docs/current/sql-insert.html).

For these three immutable tables, the repaired writer performs an ordinary SELECT, uses the existing deterministic identity's unique constraint with `ON CONFLICT (...) DO NOTHING`, and rereads skipped identities in a separate statement. A matching canonical payload becomes REUSE_NO_OP; a mismatch becomes BLOCK_CONFLICT and rolls back the enclosing runtime transaction. Other unique-constraint errors are not suppressed. No UPDATE or DELETE is issued on immutable tables. Native enrichment keeps its existing FOR UPDATE lock, exact old-value predicate and bounded UPDATE contract. Global runtime lease/fence/revision enforcement remains mandatory.

The R2 downstream adapter reports actual inserted and raced-reused counts, then independently verifies every planned physical value. It no longer mistakes a confirmed raced reuse for an incomplete INSERT. Existing numeric coercion, market/value formulas, Champion, feature order, preprocessing and Policy V1 are unchanged.

The preserved Odds response reconstructs exactly286 observations with identical books, sides, prices, source digests, timestamps, mapping IDs and game scope. The canonical market builder validates mapping ambiguity, event starts and future provider updates. The new disposable harness uses the actual runtime authority and writer with production-equivalent SELECT/INSERT grants on all three immutable tables. It verifies286 inserts,286 second-pass reuses, zero duplicate identities, immutable conflicts, real value/policy persistence, caller/readback drift, reservation integrity, started-game rejection, missing/corrupt paid evidence, stale revision/fence and immutable recovery history. A barrier interleaves two actual writer instances after both read absence; one inserts, the other rereads and reuses or blocks on changed payload. PGlite serializes SQL statements; this is an explicit race-window simulation, not a claim of separate production connections.

`resumeMarket` is a separate reviewed, service-only operation. It permits one initial42501 MARKET_PERSISTENCE recovery, only with one exact unresolved failed run, an inactive lease, same operating date, matching state and full upstream readback digests, preserved paid Odds, reconciled provider reservations and PASS DML receipts. All market targets must remain pregame with unchanged scheduled times. Predictions must retain their snapshot linkage. This initial repair requires absent observations and no value/pick rows for those predictions; unexpected partial downstream state rejects. It does not broaden recovery to other exception classes or later stages.

The operation preserves the original run ID, package, freeze, scope, predictions, mappings, counters, reservations and first failure. It records one immutable recovery entry and a specifically reviewed compatible executor SHA, then acquires a new fence. Caller checkpoints cannot change that recovery entry. Readback digest normalization retains PostgreSQL microseconds. Paid Odds are recovered through the existing private immutable evidence contract; the consumed reservation cannot be reused to make another acquisition.

Validation:9 focused R9 groups;74 physical-schema/real-feature/Champion/downstream/durable-resume checks;21 runtime-state checks;8 fenced-writer checks;5 R7/midnight checks;10 R8 recovery checks;5 R3 checks; unchanged bearer-auth handler checks; successful Next.js build. The R3 source certificate is refreshed from the exact passing real-model test inventory. No production permission broadening, migration, UI or scheduler configuration is included.

Reproduce locally with private inputs outside the repository: set `R2S_VALIDATION_DIR` to the protected validation directory containing `r9-private-inventory.json`, set `R6_PGLITE_MODULE` to the existing disposable PGlite module, then run `node scripts/mlb-phase2-r9-immutable-market-validate.mjs`. The full existing schema regression additionally requires `R1_READ_CACHE`, `BATTER_READ_CACHE` and `R2T_MULTI_READ_CACHE`. None of these inputs is published.

`MARKET_PERSISTENCE_PRODUCTION_CERTIFIED=NO`; `UI_REDESIGN_READY=NO` until production gates pass. The nineteen inherited generated-artifact modifications and reserved temporary/worktree directories are preserved.
