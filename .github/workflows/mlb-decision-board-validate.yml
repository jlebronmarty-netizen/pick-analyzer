name: MLB Decision Board Validate

on:
  push:
    branches:
      - feature/mlb-decision-board-v1
  pull_request:
    branches:
      - main
    paths:
      - 'src/app/mlb/**'
      - 'src/app/api/mlb/**'
      - 'src/components/mlb/**'
      - 'src/services/mlb-decision-board.service.ts'
      - 'src/types/mlb-decision-board.ts'
      - 'src/components/dashboard/DashboardShell.tsx'
      - '.github/workflows/mlb-decision-board-validate.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      # Build-only placeholders. They prevent import-time Supabase client construction
      # from failing during Next.js page-data collection without exposing real secrets.
      NEXT_PUBLIC_SUPABASE_URL: https://ci-placeholder.supabase.co
      NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: ci-placeholder-publishable-key
      SUPABASE_SERVICE_ROLE_KEY: ci-placeholder-service-role-key
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - run: npm run build
