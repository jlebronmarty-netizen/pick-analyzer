'use client'
export default function ErrorState({ reset }: { reset: () => void }) { return <main className="min-h-screen bg-slate-950 p-8 text-white"><h1 className="text-2xl font-semibold">Analysis is temporarily unavailable</h1><p className="mt-3 text-slate-300">We could not load the latest game data. No new recommendation is shown.</p><button onClick={reset} className="mt-5 rounded-lg bg-teal-300 px-5 py-3 font-semibold text-slate-950 focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-teal-300">Try again</button></main> }
